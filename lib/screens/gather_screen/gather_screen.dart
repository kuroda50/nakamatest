// Flutter imports:
import 'package:flutter/material.dart';

class Gather extends StatelessWidget {
  const Gather({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
